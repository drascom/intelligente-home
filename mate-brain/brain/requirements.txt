fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12.0
httpx>=0.27
aiosqlite>=0.20
pydantic-settings>=2.4
wyoming>=1.5
aiomqtt>=2.0
# FCM push (opsiyonel - yoksa dry-run modu): google-auth>=2.30
# intent fast-path (optional on dev machines - brain degrades gracefully without it)
sentence-transformers>=3.0
numpy>=1.26
# speaker-ID / voice-ID (optional - brain degrades gracefully if absent/disabled).
# Model: mate-brain/models/campplus.onnx (release'ten indirilir, README'ye bak).
sherpa-onnx>=1.12
soundfile>=0.12
# multipart upload for enrollment (FastAPI UploadFile)
python-multipart>=0.0.9
