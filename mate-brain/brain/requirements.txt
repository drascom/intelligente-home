fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12.0
httpx>=0.27
aiosqlite>=0.20
pydantic-settings>=2.4
wyoming>=1.5
aiomqtt>=2.0
# FCM push (opsiyonel - yoksa dry-run modu): google-auth>=2.30
# intent fast-path (optional on dev machines - brain degrades gracefully without it)
sentence-transformers>=3.0
numpy>=1.26
# speaker-ID / voice-ID (optional - brain degrades gracefully if absent/disabled).
# Model: mate-brain/models/campplus.onnx (release'ten indirilir, README'ye bak).
sherpa-onnx>=1.12
soundfile>=0.12
# multipart upload for enrollment (FastAPI UploadFile)
python-multipart>=0.0.9
# LiveKit server-side ses agent'ı (deneysel; LIVEKIT_AGENT_ENABLED flag arkasında).
# Flag kapalıyken import edilmez; sadece /api/livekit-token endpoint'i (secret
# ayarlıysa) livekit-api'yi tembel import eder.
livekit>=1.1.10
livekit-api>=1.1.0
# Smart Turn v3 ses-tabanlı EOU (opsiyonel; TURN_DETECTOR_ENABLED flag arkasında).
# onnxruntime: ~8MB ONNX modeli çalıştırır. transformers: WhisperFeatureExtractor
# (numpy tabanlı log-mel; torch GEREKMEZ). Model HF'den tembel indirilir
# (pipecat-ai/smart-turn-v3). Flag kapalıyken hiçbiri import edilmez (fail-open).
onnxruntime>=1.20
transformers>=4.44
