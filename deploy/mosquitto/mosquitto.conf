persistence true
persistence_location /mosquitto/data/

listener 1883
allow_anonymous false
password_file /mosquitto/passwd/passwd

# Per-device credentials are created with:
#   docker exec mosquitto mosquitto_passwd -b /mosquitto/passwd/passwd <user> <pass>
# then: docker kill -s HUP mosquitto
